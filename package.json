{
  "name": "budgetplanner",
  "version": "1.0.0",
  "scripts": {
    "start": "live-server",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "firebase": "^11.3.1"
  },
  "devDependencies": {
    "crypto-browserify": "^3.12.1",
    "live-server": "^1.2.2"
  }
}
